export const setupLinkMessage = () =>{
    `
          <h2>Hello ${user.name}</h2>
          <p>You have been added to the system. Please set your password:</p>
          <a href="${resetUrl}" clicktracking=off>${resetUrl}</a>
        `
}


